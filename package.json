{
  "name": "research-timeline",
  "version": "1.0.0",
  "private": true,
  "description": "Local web app that polls top journals (via PubMed) and shows a per-disease timeline of recent research.",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client",
    "start": "npm run build -w client && npm run start -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
